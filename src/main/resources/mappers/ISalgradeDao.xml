<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zhuoxun.it.business.dao.ISalgradeDao">

    <!-- 基础字段返回信息配置 -->
    <resultMap id="BaseResultMap" type="com.zhuoxun.it.business.entity.SalgradeVO">
        <id column="grade" jdbcType="INTEGER" property="grade" />
        <result column="losal" jdbcType="INTEGER" property="losal" />
        <result column="hisal" jdbcType="INTEGER" property="hisal" />
    </resultMap>

    <!-- 基础字段信息配置 -->
    <sql id="Base_Column_List">
        grade,losal,hisal,
    </sql>

    <!-- 基础条件查询配置 -->
    <sql id="Base_Where_Clause">
        <where>
            <if test="grade != null">
                and grade = #{entity.grade,jdbcType=INTEGER}
            </if>
            <if test="losal != null">
                and losal = #{entity.losal,jdbcType=INTEGER}
            </if>
            <if test="hisal != null">
                and hisal = #{entity.hisal,jdbcType=INTEGER}
            </if>
        </where>
    </sql>

    <!-- 根据主键信息查询返回实体对象 -->
    <select id="findById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from emp
        where grade = #{grade,jdbcType=INTEGER}
    </select>

    <!-- 根据实体对象信息查询返回实体对象 -->
    <select id="select" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from salgrade
        <if test="_parameter != null">
            <include refid="Base_Where_Clause" />
        </if>
    </select>

    <!-- 根据实体对象信息查询返回实体对象集合 -->
    <select id="findList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from salgrade
        where empno = #{id,jdbcType=INTEGER}
        <if test="_parameter != null">
            <include refid="Base_Where_Clause" />
        </if>
    </select>

    <!-- 根据主键信息删除实体对象 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from salgrade
        where grade = #{grade,jdbcType=INTEGER}
    </delete>

    <!-- 插入实体对象 -->
    <insert id="insert" parameterType="com.frame.business.entity.SalgradeVO">
        insert into salgrade
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="grade != null">
                grade,
            </if>
            <if test="losal != null">
                losal,
            </if>
            <if test="hisal != null">
                hisal,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="grade != null">
                #{grade,jdbcType=INTEGER},
            </if>
            <if test="losal != null">
                #{losal,jdbcType=INTEGER},
            </if>
            <if test="hisal != null">
                #{hisal,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <!-- 更新实体对象 -->
    <update id="update" parameterType="com.zhuoxun.it.business.entity.SalgradeVO">
        update salgrade
        <set>
            <if test="grade != null">
                grade = #{grade,jdbcType=INTEGER},
            </if>
            <if test="losal != null">
                losal = #{losal,jdbcType=INTEGER},
            </if>
            <if test="hisal != null">
                hisal = #{hisal,jdbcType=INTEGER},
            </if>
        </set>
        where grade = #{grade,jdbcType=INTEGER}
    </update>

</mapper>
