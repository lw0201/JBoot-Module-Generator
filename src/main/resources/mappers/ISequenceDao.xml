<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jboot.generator.dao.ISequenceDao">

    <!--基础字段返回信息配置 -->
    <resultMap id="BaseResultMap" type="org.jboot.generator.entity.SequenceVO">
        <id column="name" jdbcType="VARCHAR" property="name" />
        <result column="current_value" jdbcType="INTEGER" property="currentValue" />
        <result column="increment" jdbcType="INTEGER" property="increment" />
    </resultMap>

    <!--基础字段信息配置 -->
    <sql id="Base_Column_List">
        <choose>
            <when test="_parameter.containsKey('columns') and columns != null and columns.length>0">
                <include refid="Base_Auto_Cloumns" />
            </when>
            <otherwise>
                t.`name`,t.`current_value`,t.`increment`
            </otherwise>
        </choose>
    </sql>

    <!--自定义字段查询 -->
    <sql id="Base_Auto_Cloumns">
        <trim prefixOverrides=",">
            <foreach collection="columns" item="column">
                <choose>
                    <when test="column == 'name'">
                        ,t.name
                    </when>
                    <when test="column == 'currentValue'">
                        ,t.current_value
                    </when>
                    <when test="column == 'increment'">
                        ,t.increment
                    </when>
                </choose>
            </foreach>
        </trim>
    </sql>

    <!--基础条件查询配置 -->
    <sql id="Base_Where_Clause">
        <where>
            <if test="entity.name != null">
                and t.`name` = #{entity.name,jdbcType=VARCHAR}
            </if>
            <if test="entity.currentValue != null">
                and t.`current_value` = #{entity.currentValue,jdbcType=INTEGER}
            </if>
            <if test="entity.increment != null">
                and t.`increment` = #{entity.increment,jdbcType=INTEGER}
            </if>
        </where>
    </sql>

    <!--构造条件 -->
    <sql id="Wrapper_Where_Clause">
        <where>
            <foreach collection="wp.criterias" item="criteria" separator="or">
                <trim prefix="(" prefixOverrides="and" suffix=")">
                    <foreach collection="criteria.criterions" item="criterion">
                         <include refid="Wrapper_Where_Op_Clause"></include>
                    </foreach>
                </trim>
            </foreach>
        </where>
    </sql>

    <!--构造操作条件 -->
    <sql id="Wrapper_Where_Op_Clause">
        <choose>
            <when test="criterion.op.type == 'like' or criterion.op.type == 'notLike'">
                <choose>
                    <when test="criterion.attr == 'name'">
                        and LOWER(t.`name`)
                        <include refid="Wrapper_Op_Clause" />
                    </when>
                    <when test="criterion.attr == 'currentValue'">
                        and LOWER(t.`current_value`)
                        <include refid="Wrapper_Op_Clause" />
                    </when>
                    <when test="criterion.attr == 'increment'">
                        and LOWER(t.`increment`)
                        <include refid="Wrapper_Op_Clause" />
                    </when>
                    <otherwise>
                        and false
                    </otherwise>
                </choose>
            </when>
            <otherwise>
                <choose>
                    <when test="criterion.attr == 'name'">
                        and t.`name`
                        <include refid="Wrapper_Op_Clause" />
                    </when>
                    <when test="criterion.attr == 'currentValue'">
                        and t.`current_value`
                        <include refid="Wrapper_Op_Clause" />
                    </when>
                    <when test="criterion.attr == 'increment'">
                        and t.`increment`
                        <include refid="Wrapper_Op_Clause" />
                    </when>
                    <otherwise>
                        and false
                    </otherwise>
                </choose>
            </otherwise>
        </choose>
    </sql>

    <!--查询操作构造器 -->
    <sql id="Wrapper_Op_Clause">
        <choose>
            <when test="criterion.op.type == 'eq'">
                = #{criterion.value}
            </when>
            <when test="criterion.op.type == 'ne'">
                != #{criterion.value}
            </when>
            <when test="criterion.op.type == 'gt'">
                <![CDATA[ > #{criterion.value} ]]>
            </when>
            <when test="criterion.op.type == 'ge'">
                <![CDATA[ >= #{criterion.value} ]]>
            </when>
            <when test="criterion.op.type == 'lt'">
                <![CDATA[ < #{criterion.value} ]]>
            </when>
            <when test="criterion.op.type == 'le'">
                <![CDATA[ <= #{criterion.value} ]]>
            </when>
            <when test="criterion.op.type == 'like'">
                <![CDATA[ LIKE CONCAT('%',LOWER(#{criterion.value}),'%')]]>
            </when>
            <when test="criterion.op.type == 'notLike'">
                <![CDATA[ NOT LIKE CONCAT('%',LOWER(#{criterion.value}),'%') ]]>
            </when>
            <when test="criterion.op.type == 'isNull'">
                is null
            </when>
            <when test="criterion.op.type == 'isNotNull'">
                is not null
            </when>
            <when test="criterion.op.type == 'in'">
                in
                <foreach collection="criterion.value" item="value" separator="," open="(" close=")">
                    #{value}
                </foreach>
            </when>
            <when test="criterion.op.type == 'notIn'">
                not in
                <foreach collection="criterion.value" item="value" separator="," open="(" close=")">
                    #{value}
                </foreach>
            </when>
            <when test="criterion.op.type == 'between'">
                BETWEEN #{criterion.value} and #{criterion.secondValue}
            </when>
            <when test="criterion.op.type == 'notBetween'">
                NOT BETWEEN #{criterion.value} and #{criterion.secondValue}
            </when>
        </choose>
    </sql>

    <!--构造排序条件 -->
    <sql id="Wrapper_Order_Clause">
        <if test="wp.sorts != null">
            <foreach collection="wp.sorts" open=" order by " item="st" separator=",">
                <choose>
                    <when test="st.attr == 'name'">
                        t.`name` ${st.order.type}
                    </when>
                    <when test="st.attr == 'currentValue'">
                        t.`current_value` ${st.order.type}
                    </when>
                    <when test="st.attr == 'increment'">
                        t.`increment` ${st.order.type}
                    </when>
                </choose>
            </foreach>
        </if>
    </sql>

    <!--根据主键信息查询返回实体对象 -->
    <select id="findById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from `sequence` t
        where t.`name` = #{name,jdbcType=VARCHAR}
    </select>

    <!--根据实体对象信息查询返回实体对象 -->
    <select id="query" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from `sequence` t
        <include refid="Base_Where_Clause" />
    </select>

    <!--根据实体对象信息查询返回实体对象集合 -->
    <select id="findList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from `sequence` t
        <if test="_parameter != null">
            <include refid="Base_Where_Clause" />
        </if>
    </select>
    
    <!--根据主键信息删除实体对象 -->
    <delete id="deleteById" parameterType="java.io.Serializable">
        delete t from `sequence` t
        where t.`name` = #{name,jdbcType=VARCHAR}
    </delete>
    
    <!--根据实体对象删除数据-->
    <delete id="delete" parameterType="org.jboot.generator.entity.SequenceVO">
        delete t from `sequence` t
        <include refid="Base_Where_Clause" />
    </delete>

    <!--根据实体对象删除数据-->
    <delete id="deletes" parameterType="org.jboot.generator.entity.SequenceVO">
        delete t from `sequence` t where t.`name` in
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item.name}
        </foreach>
    </delete>

    <!--插入实体对象 -->
    <insert id="insert" parameterType="org.jboot.generator.entity.SequenceVO">
        <selectKey order="BEFORE" resultType="java.lang.String" keyProperty="name">
            select uuid() 
        </selectKey>
        insert into `sequence`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">
                `name`,
            </if>
            <if test="currentValue != null">
                `current_value`,
            </if>
            <if test="increment != null">
                `increment`,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null">
                #{name,jdbcType=VARCHAR},
            </if>
            <if test="currentValue != null">
                #{currentValue,jdbcType=INTEGER},
            </if>
            <if test="increment != null">
                #{increment,jdbcType=INTEGER},
            </if>
        </trim>
    </insert>

    <!--插入实体对象 -->
    <insert id="inserts" parameterType="org.jboot.generator.entity.SequenceVO">
        insert into `sequence`
        <trim prefix="(" suffix=") values" suffixOverrides=",">
            `name`,
            `current_value`,
            `increment`
        </trim>
        <foreach collection="list" item="item" index="index" separator=",">
            (
            UUID(),
            #{item.currentValue,jdbcType=INTEGER},
            #{item.increment,jdbcType=INTEGER}
            )
        </foreach>
    </insert>

    <!--更新实体对象 -->
    <update id="update">
        update `sequence` t
        <set>
            <if test="entity.currentValue != null">
                t.`current_value` = #{entity.currentValue,jdbcType=INTEGER},
            </if>
            <if test="entity.increment != null">
                t.`increment` = #{entity.increment,jdbcType=INTEGER},
            </if>
        </set>
        where t.`name` = #{entity.name,jdbcType=VARCHAR}
    </update>

    <!--构造器查询 -->
    <select id="queryByWrapper" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from `sequence` t
        <if test="wp != null">
            <include refid="Wrapper_Where_Clause" />
        </if>
    </select>
    
    <!--构造器查询 -->
    <select id="findByWrapper" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from `sequence` t
        <if test="wp != null">
            <include refid="Wrapper_Where_Clause" />
            <include refid="Wrapper_Order_Clause" />
        </if>
    </select>

    <!--更新实体对象 -->
    <update id="updateByWrapper">
        update `sequence` t
        <set>
            <if test="entity.name != null">
                t.`name` = #{entity.name,jdbcType=VARCHAR},
            </if>
            <if test="entity.currentValue != null">
                t.`current_value` = #{entity.currentValue,jdbcType=INTEGER},
            </if>
            <if test="entity.increment != null">
                t.`increment` = #{entity.increment,jdbcType=INTEGER},
            </if>
        </set>
        <include refid="Wrapper_Where_Clause" />
    </update>
    
    <!--根据实体对象删除数据-->
    <delete id="deleteByWrapper">
        delete t from `sequence` t
        <include refid="Wrapper_Where_Clause" />
    </delete>

</mapper>