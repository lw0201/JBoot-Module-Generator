<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jboot.generator.dao.ISysConfigDao">

    <!-- 基础字段返回信息配置 -->
    <resultMap id="BaseResultMap" type="org.jboot.generator.entity.SysConfigVO">
        <id column="variable" jdbcType="VARCHAR" property="variable" />
        <result column="value" jdbcType="VARCHAR" property="value" />
        <result column="set_time" jdbcType="DATE" property="setTime" />
        <result column="set_by" jdbcType="VARCHAR" property="setBy" />
    </resultMap>

    <!-- 基础字段信息配置 -->
    <sql id="Base_Column_List">
        variable,value,set_time,set_by
    </sql>

    <!-- 基础条件查询配置 -->
    <sql id="Base_Where_Clause">
        <where>
            <if test="variable != null">
                and variable = #{entity.variable,jdbcType=VARCHAR}
            </if>
            <if test="value != null">
                and value = #{entity.value,jdbcType=VARCHAR}
            </if>
            <if test="setTime != null">
                and set_time = #{entity.setTime,jdbcType=DATE}
            </if>
            <if test="setBy != null">
                and set_by = #{entity.setBy,jdbcType=VARCHAR}
            </if>
        </where>
    </sql>

    <!-- 根据主键信息查询返回实体对象 -->
    <select id="findById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from emp
        where variable = #{variable,jdbcType=VARCHAR}
    </select>

    <!-- 根据实体对象信息查询返回实体对象 -->
    <select id="select" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from sys_config
        <if test="_parameter != null">
            <include refid="Base_Where_Clause" />
        </if>
    </select>

    <!-- 根据实体对象信息查询返回实体对象集合 -->
    <select id="findList" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from sys_config
        <if test="_parameter != null">
            <include refid="Base_Where_Clause" />
        </if>
    </select>

    <!-- 根据主键信息删除实体对象 -->
    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from sys_config
        where variable = #{variable,jdbcType=VARCHAR}
    </delete>

    <!-- 插入实体对象 -->
    <insert id="insert" parameterType="org.jboot.generator.entity.SysConfigVO">
        insert into sys_config
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="variable != null">
                variable,
            </if>
            <if test="value != null">
                value,
            </if>
            <if test="setTime != null">
                set_time,
            </if>
            <if test="setBy != null">
                set_by,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="variable != null">
                #{variable,jdbcType=VARCHAR},
            </if>
            <if test="value != null">
                #{value,jdbcType=VARCHAR},
            </if>
            <if test="setTime != null">
                #{setTime,jdbcType=DATE},
            </if>
            <if test="setBy != null">
                #{setBy,jdbcType=VARCHAR},
            </if>
        </trim>
    </insert>

    <!-- 更新实体对象 -->
    <update id="update" parameterType="org.jboot.generator.entity.SysConfigVO">
        update sys_config
        <set>
            <if test="variable != null">
                variable = #{variable,jdbcType=VARCHAR},
            </if>
            <if test="value != null">
                value = #{value,jdbcType=VARCHAR},
            </if>
            <if test="setTime != null">
                set_time = #{setTime,jdbcType=DATE},
            </if>
            <if test="setBy != null">
                set_by = #{setBy,jdbcType=VARCHAR},
            </if>
        </set>
        where variable = #{variable,jdbcType=VARCHAR}
    </update>

</mapper>
